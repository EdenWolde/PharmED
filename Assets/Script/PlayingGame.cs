using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayingGame
{
    /*
    public static void CheckAnswer(GameObject TappedGameObject)
    {
        if (TappedGameObject.GetComponent<DrugInfoClass>().genericName == LG.DrugOnQuest.GetComponent<DrugInfoClass>().genericName)                       //When user finds the drug in quest
        {
            LG.DrugFound = true;
            // GenerateQuestion();
        }
        else                                                                                                                                           // Display(Say) to try again 
        {
            TappedGameObject.GetComponent<Rigidbody>().useGravity = true;
        }
    }*/
}
